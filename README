/config/php/php.ini       replacable
/config/php/fpm/pool.d/   phpfpm configs
/config/httpd/conf.d/  apache configs



ENV VARS:

php.<PHPKEY>  variables are injected into the container php.ini
fpm.global.<KEY> variables are injected into global php-fpm.conf
fpm.pool.<KEY> variables are injected into pool "www" fpm config.
APACHE_MODULES space seperated list of modules for apache to load

